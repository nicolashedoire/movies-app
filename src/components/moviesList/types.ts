export interface IMovie {
    allocine_id: string;
    created_at: string;
    creation_date: string;
    director: string;
    duration: string;
    genders: Array<string>;
    id: string;
    image: string;
    synopsis: string;
    title: string;
}
